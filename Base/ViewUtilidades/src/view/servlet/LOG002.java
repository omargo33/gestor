package view.servlet;import java.io.IOException;import java.io.PrintWriter;import java.util.logging.Level;import java.util.logging.Logger;import javax.servlet.ServletConfig;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;/** * Objeto para enviar el usuario y token a sistemas. * * @author omargo33@hotmail.com */@WebServlet(name = "LOG002", urlPatterns = "/LOG002")public class LOG002 extends HttpServlet {    @SuppressWarnings("compatibility:-1723834327486045707")    private static final long serialVersionUID = 1L;    public void init(ServletConfig config) throws ServletException {        super.init(config);    }    /**     * Metodo para responder do GET.     *     * @param request     * @param response     * @throws ServletException     * @throws IOException     */    public void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        solicitarURL(request, response);    }    /**     * Metodo para solicitar url Segura.     *     * @param request     * @param response     */    private void solicitarURL(HttpServletRequest request, HttpServletResponse response) {        boolean estado = true;        HttpSession session = request.getSession(true);        String url = String.valueOf(session.getAttribute("url"));        String token = String.valueOf(session.getAttribute("token"));        //String token = request.getHeader("Authorization");        try {            response.setContentType("text/html");            PrintWriter pw = response.getWriter();            url = url.replace("&?", "&");            url = url + token;            Logger.getLogger(Logger.GLOBAL_LOGGER_NAME).log(Level.WARNING, "VALIDAR envio " + url);            response.setHeader("Authorization", "Bearer " + token);            response.sendRedirect(url);            pw.close();        } catch (IOException e) {            estado = false;        }        if (!estado) {            try {                response.sendError(HttpServletResponse.SC_UNAUTHORIZED);            } catch (IOException e) {                Logger.getLogger(Logger.GLOBAL_LOGGER_NAME).log(Level.WARNING, e.toString());            }        }    }}